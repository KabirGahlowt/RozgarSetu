# CORS is auto-allowed for http://<LAN-IP>:5173 in dev. Add more origins only if needed:
# CORS_ORIGINS=https://myapp.com

HOST=0.0.0.0
PORT=8000
MONGO_URI=
SECRET_KEY=

# Render sets RENDER=true and often NODE_ENV=production; auth cookies use SameSite=None + Secure when either is set.
