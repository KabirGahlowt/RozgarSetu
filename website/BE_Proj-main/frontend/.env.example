# Optional — only if you are NOT using the Vite dev proxy (e.g. production build).
# Leave unset during `npm run dev` so the app uses /api/v1 and /rs-bot-proxy (see vite.config.js).
#
# VITE_API_BASE_URL=http://localhost:8000
# VITE_RS_BOT_URL=http://localhost:8001
